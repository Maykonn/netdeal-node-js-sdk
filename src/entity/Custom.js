const Entity = require('./Entity.js');

class Custom extends Entity {

  /**
   * This entity could have any string as key, for example PRODUCT, VIEW, etc.
   */
  constructor() {
    super();
  }

}

module.exports = Custom;
